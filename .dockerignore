# 排除不需要的檔案和目錄
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 排除版本控制
.git
.gitignore

# 排除環境檔案（將在 docker-compose 中處理）
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 排除建構輸出
dist
build

# 排除 Docker 相關檔案
Dockerfile*
docker-compose*
.dockerignore

# 排除 IDE 和編輯器檔案
.vscode
.idea
*.swp
*.swo
*~

# 排除作業系統產生的檔案
.DS_Store
Thumbs.db

# 排除測試覆蓋率報告
coverage

# 排除日誌檔案
*.log
logs

# 排除暫存檔案
*.tmp
*.temp
